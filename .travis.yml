sudo: false
language: node_js
cache:
  direcotories:
    - node_modules
script:
  - npm run lint
deploy:
  provider: npm
  email: "lexswed@gmail.com"
  api_key: $NPM_TOKEN
  on:
    branch: master
after_success:
  - npm install -g travis-deploy-once
  - travis-deploy-once "npm run semantic-release"